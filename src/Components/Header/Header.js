import Navbar from "../Navbar/Navbar";
import './Header.css';

export default function Header() {
    return (
        <header className="header__container">
            <Navbar />
        </header>
    );
}